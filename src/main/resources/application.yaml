useMock: true
mockURL: https://serene-coast-56441.herokuapp.com/api/users/

db:
  driver: org.postgresql.Driver
  password: 1d9cf3e01fe8e49b0125fbcc08405cfa1c56a25db98c03dca269df5c28a685c5
  url: jdbc:postgresql://ec2-54-228-209-117.eu-west-1.compute.amazonaws.com:5432/dbto94h2bqfj9k
  username: ncppuwgfnpiftv
entitymanager:
  packagesToScan: com
hibernate:
  dialect: org.hibernate.dialect.PostgreSQLDialect
  hbm2ddl:
    auto: none
  show_sql: true

spring:
  datasource:
    base: ${db.url}
    driver-class-name: ${db.driver}
    password: ${db.password}
    tomcat:
      test-on-borrow: true
    url: ${spring.datasource.base}
    username: ${db.username}
    validation-query: select 1
  liquibase:
    change-log: classpath:/db/db.changelog-master.xml
    contexts: postgres,text
    password: ${spring.datasource.password}
    url: ${spring.datasource.url}
    user: ${spring.datasource.username}
